{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"D:/角色卡/VS大前端/200719X/bbq66686\" && git add -A && (git diff --cached --quiet || (git commit -m \"auto: update\" && git push))",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
